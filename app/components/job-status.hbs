<span
  {{did-update (perform this.monitorJobProgress @job) @job}}
  {{did-insert (perform this.monitorJobProgress @job)}}
>
  {{#if @job.succeeded}}
    ✅
  {{else if @job.failed}}
    🟥
  {{else}}
    ⏳
  {{/if}}
</span>