{{#if @model.length}}
  <ul>
    {{#each @model as |vocab|}}
      <li><LinkTo @route="vocabulary" @model={{vocab.id}}>{{vocab.name}}</LinkTo><button
          type='button'
          {{on 'click' (fn this.deleteVocab vocab)}}
        >🗑️</button><button
          type='button'
          {{on 'click' (fn this.createDownloadJob vocab.uri)}}
        >{{this.downloadBtnTxt}}</button></li>
    {{/each}}
  </ul>
{{else}}
  <p>No vocabularies were found</p>
{{/if}}
<AddVocab />